{
  "name": "@monoprefix/ui-components",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "peerDependencies": {
    "react": "^16.11.0",
    "styled-components": "4.0.0>="
  },
  "scripts": {
    "build:types": "npx swc src --out-dir target --sync",
    "build:js": "tsc --pretty --skipLibCheck --noEmit",
    "build": "yarn build:types && yarn build:js"
  }
}
